ToolNest PDF Conversion - restored native method + scanned PDF visual fix

Baseline:
- Restored the earlier native/text PDF conversion approach.
- Native PDFs use PyMuPDF + pdfplumber for editable text and real Word tables.
- Repeated letterheads/footers are preserved as Word header/footer elements.

Scanned/image-only PDFs:
- The previous VML OCR-overlay path could render blank in common DOCX renderers.
- Scanned pages now use the original scan as a page-anchored Word picture.
- Each scanned page is preserved visually and remains selectable/editable as a Word image.
- OCR remains available through the TXT and HTML conversion paths, where extracted text is directly editable.
- This avoids corrupting certificates, cursive text, signatures, photos and complex tables with unreliable OCR overlays.

Validation performed locally:
1. PersonalStatement_MC-PUBHL.pdf -> DOCX/TXT/HTML
   - DOCX rendered successfully as 2 pages and visually preserved the Curtin form.
2. HSSC(3).pdf -> DOCX/TXT/HTML
   - DOCX rendered successfully as 2 pages and visually preserved both scanned certificate pages.
   - The previous blank DOCX result was eliminated.
3. Native refusal-letter PDF -> DOCX
   - DOCX rendered successfully as 6 pages.
   - 2 real Word tables remained present.
4. Python syntax check completed successfully.
